{
  "name": "jarvis-agent-cli",
  "version": "1.1.0",
  "description": "Jarvis Agent in your terminal — the same agentic engine as the VS Code extension, as a CLI. Multi-provider agentic chat (local or cloud), tools + HITL, checkpoints, RAG, specialized agents, workflows and Auto-TDD.",
  "type": "module",
  "bin": {
    "jarvis": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "ai",
    "agent",
    "cli",
    "coding-assistant",
    "ollama",
    "openrouter",
    "llm",
    "rag",
    "mcp",
    "jarvis"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/maxoubvd/extension-VS-code.git"
  },
  "scripts": {
    "build": "node build-cli.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@jarvis/core": "*",
    "@inquirer/prompts": "^7.2.0",
    "marked": "^12.0.0",
    "marked-terminal": "^7.2.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/marked-terminal": "^6.1.1"
  }
}
